/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { LET_ME_BUY_PROGRAM_ADDRESS } from "../programs";

export const DELETE_PRODUCT_DISCRIMINATOR = new Uint8Array([
  173, 212, 141, 230, 33, 82, 166, 25,
]);

export function getDeleteProductDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    DELETE_PRODUCT_DISCRIMINATOR,
  );
}

export type DeleteProductInstruction<
  TProgram extends string = typeof LET_ME_BUY_PROGRAM_ADDRESS,
  TAccountReceipts extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountReceipts extends string
        ? WritableAccount<TAccountReceipts>
        : TAccountReceipts,
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type DeleteProductInstructionData = {
  discriminator: ReadonlyUint8Array;
  storeName: string;
  productName: string;
};

export type DeleteProductInstructionDataArgs = {
  storeName: string;
  productName: string;
};

export function getDeleteProductInstructionDataEncoder(): Encoder<DeleteProductInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["storeName", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["productName", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
    ]),
    (value) => ({ ...value, discriminator: DELETE_PRODUCT_DISCRIMINATOR }),
  );
}

export function getDeleteProductInstructionDataDecoder(): Decoder<DeleteProductInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["storeName", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["productName", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
  ]);
}

export function getDeleteProductInstructionDataCodec(): Codec<
  DeleteProductInstructionDataArgs,
  DeleteProductInstructionData
> {
  return combineCodec(
    getDeleteProductInstructionDataEncoder(),
    getDeleteProductInstructionDataDecoder(),
  );
}

export type DeleteProductAsyncInput<
  TAccountReceipts extends string = string,
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  receipts?: Address<TAccountReceipts>;
  authority: TransactionSigner<TAccountAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
  storeName: DeleteProductInstructionDataArgs["storeName"];
  productName: DeleteProductInstructionDataArgs["productName"];
};

export async function getDeleteProductInstructionAsync<
  TAccountReceipts extends string,
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof LET_ME_BUY_PROGRAM_ADDRESS,
>(
  input: DeleteProductAsyncInput<
    TAccountReceipts,
    TAccountAuthority,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  DeleteProductInstruction<
    TProgramAddress,
    TAccountReceipts,
    TAccountAuthority,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? LET_ME_BUY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    receipts: { value: input.receipts ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.receipts.value) {
    accounts.receipts.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([114, 101, 99, 101, 105, 112, 116, 115]),
        ),
        getUtf8Encoder().encode(
          getNonNullResolvedInstructionInput("storeName", args.storeName),
        ),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("receipts", accounts.receipts),
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getDeleteProductInstructionDataEncoder().encode(
      args as DeleteProductInstructionDataArgs,
    ),
    programAddress,
  } as DeleteProductInstruction<
    TProgramAddress,
    TAccountReceipts,
    TAccountAuthority,
    TAccountSystemProgram
  >);
}

export type DeleteProductInput<
  TAccountReceipts extends string = string,
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  receipts: Address<TAccountReceipts>;
  authority: TransactionSigner<TAccountAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
  storeName: DeleteProductInstructionDataArgs["storeName"];
  productName: DeleteProductInstructionDataArgs["productName"];
};

export function getDeleteProductInstruction<
  TAccountReceipts extends string,
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof LET_ME_BUY_PROGRAM_ADDRESS,
>(
  input: DeleteProductInput<
    TAccountReceipts,
    TAccountAuthority,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): DeleteProductInstruction<
  TProgramAddress,
  TAccountReceipts,
  TAccountAuthority,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? LET_ME_BUY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    receipts: { value: input.receipts ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("receipts", accounts.receipts),
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getDeleteProductInstructionDataEncoder().encode(
      args as DeleteProductInstructionDataArgs,
    ),
    programAddress,
  } as DeleteProductInstruction<
    TProgramAddress,
    TAccountReceipts,
    TAccountAuthority,
    TAccountSystemProgram
  >);
}

export type ParsedDeleteProductInstruction<
  TProgram extends string = typeof LET_ME_BUY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    receipts: TAccountMetas[0];
    authority: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: DeleteProductInstructionData;
};

export function parseDeleteProductInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedDeleteProductInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      receipts: getNextAccount(),
      authority: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getDeleteProductInstructionDataDecoder().decode(instruction.data),
  };
}
