/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { LET_ME_BUY_PROGRAM_ADDRESS } from "../programs";

export const UPDATE_TELEGRAM_CHANNEL_DISCRIMINATOR = new Uint8Array([
  125, 26, 242, 114, 17, 12, 103, 200,
]);

export function getUpdateTelegramChannelDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UPDATE_TELEGRAM_CHANNEL_DISCRIMINATOR,
  );
}

export type UpdateTelegramChannelInstruction<
  TProgram extends string = typeof LET_ME_BUY_PROGRAM_ADDRESS,
  TAccountReceipts extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountReceipts extends string
        ? WritableAccount<TAccountReceipts>
        : TAccountReceipts,
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateTelegramChannelInstructionData = {
  discriminator: ReadonlyUint8Array;
  storeName: string;
  telegramChannelId: string;
};

export type UpdateTelegramChannelInstructionDataArgs = {
  storeName: string;
  telegramChannelId: string;
};

export function getUpdateTelegramChannelInstructionDataEncoder(): Encoder<UpdateTelegramChannelInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["storeName", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      [
        "telegramChannelId",
        addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder()),
      ],
    ]),
    (value) => ({
      ...value,
      discriminator: UPDATE_TELEGRAM_CHANNEL_DISCRIMINATOR,
    }),
  );
}

export function getUpdateTelegramChannelInstructionDataDecoder(): Decoder<UpdateTelegramChannelInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["storeName", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    [
      "telegramChannelId",
      addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder()),
    ],
  ]);
}

export function getUpdateTelegramChannelInstructionDataCodec(): Codec<
  UpdateTelegramChannelInstructionDataArgs,
  UpdateTelegramChannelInstructionData
> {
  return combineCodec(
    getUpdateTelegramChannelInstructionDataEncoder(),
    getUpdateTelegramChannelInstructionDataDecoder(),
  );
}

export type UpdateTelegramChannelAsyncInput<
  TAccountReceipts extends string = string,
  TAccountAuthority extends string = string,
> = {
  receipts?: Address<TAccountReceipts>;
  authority: TransactionSigner<TAccountAuthority>;
  storeName: UpdateTelegramChannelInstructionDataArgs["storeName"];
  telegramChannelId: UpdateTelegramChannelInstructionDataArgs["telegramChannelId"];
};

export async function getUpdateTelegramChannelInstructionAsync<
  TAccountReceipts extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof LET_ME_BUY_PROGRAM_ADDRESS,
>(
  input: UpdateTelegramChannelAsyncInput<TAccountReceipts, TAccountAuthority>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  UpdateTelegramChannelInstruction<
    TProgramAddress,
    TAccountReceipts,
    TAccountAuthority
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? LET_ME_BUY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    receipts: { value: input.receipts ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.receipts.value) {
    accounts.receipts.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([114, 101, 99, 101, 105, 112, 116, 115]),
        ),
        getUtf8Encoder().encode(
          getNonNullResolvedInstructionInput("storeName", args.storeName),
        ),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("receipts", accounts.receipts),
      getAccountMeta("authority", accounts.authority),
    ],
    data: getUpdateTelegramChannelInstructionDataEncoder().encode(
      args as UpdateTelegramChannelInstructionDataArgs,
    ),
    programAddress,
  } as UpdateTelegramChannelInstruction<
    TProgramAddress,
    TAccountReceipts,
    TAccountAuthority
  >);
}

export type UpdateTelegramChannelInput<
  TAccountReceipts extends string = string,
  TAccountAuthority extends string = string,
> = {
  receipts: Address<TAccountReceipts>;
  authority: TransactionSigner<TAccountAuthority>;
  storeName: UpdateTelegramChannelInstructionDataArgs["storeName"];
  telegramChannelId: UpdateTelegramChannelInstructionDataArgs["telegramChannelId"];
};

export function getUpdateTelegramChannelInstruction<
  TAccountReceipts extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof LET_ME_BUY_PROGRAM_ADDRESS,
>(
  input: UpdateTelegramChannelInput<TAccountReceipts, TAccountAuthority>,
  config?: { programAddress?: TProgramAddress },
): UpdateTelegramChannelInstruction<
  TProgramAddress,
  TAccountReceipts,
  TAccountAuthority
> {
  // Program address.
  const programAddress = config?.programAddress ?? LET_ME_BUY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    receipts: { value: input.receipts ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("receipts", accounts.receipts),
      getAccountMeta("authority", accounts.authority),
    ],
    data: getUpdateTelegramChannelInstructionDataEncoder().encode(
      args as UpdateTelegramChannelInstructionDataArgs,
    ),
    programAddress,
  } as UpdateTelegramChannelInstruction<
    TProgramAddress,
    TAccountReceipts,
    TAccountAuthority
  >);
}

export type ParsedUpdateTelegramChannelInstruction<
  TProgram extends string = typeof LET_ME_BUY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    receipts: TAccountMetas[0];
    authority: TAccountMetas[1];
  };
  data: UpdateTelegramChannelInstructionData;
};

export function parseUpdateTelegramChannelInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUpdateTelegramChannelInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { receipts: getNextAccount(), authority: getNextAccount() },
    data: getUpdateTelegramChannelInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
