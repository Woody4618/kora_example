/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { LET_ME_BUY_PROGRAM_ADDRESS } from "../programs";

export const MARK_AS_DELIVERED_DISCRIMINATOR = new Uint8Array([
  112, 46, 230, 68, 152, 97, 194, 62,
]);

export function getMarkAsDeliveredDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    MARK_AS_DELIVERED_DISCRIMINATOR,
  );
}

export type MarkAsDeliveredInstruction<
  TProgram extends string = typeof LET_ME_BUY_PROGRAM_ADDRESS,
  TAccountReceipts extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountReceipts extends string
        ? WritableAccount<TAccountReceipts>
        : TAccountReceipts,
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type MarkAsDeliveredInstructionData = {
  discriminator: ReadonlyUint8Array;
  storeName: string;
  receiptId: bigint;
};

export type MarkAsDeliveredInstructionDataArgs = {
  storeName: string;
  receiptId: number | bigint;
};

export function getMarkAsDeliveredInstructionDataEncoder(): Encoder<MarkAsDeliveredInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["storeName", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["receiptId", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: MARK_AS_DELIVERED_DISCRIMINATOR }),
  );
}

export function getMarkAsDeliveredInstructionDataDecoder(): Decoder<MarkAsDeliveredInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["storeName", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["receiptId", getU64Decoder()],
  ]);
}

export function getMarkAsDeliveredInstructionDataCodec(): Codec<
  MarkAsDeliveredInstructionDataArgs,
  MarkAsDeliveredInstructionData
> {
  return combineCodec(
    getMarkAsDeliveredInstructionDataEncoder(),
    getMarkAsDeliveredInstructionDataDecoder(),
  );
}

export type MarkAsDeliveredAsyncInput<
  TAccountReceipts extends string = string,
  TAccountAuthority extends string = string,
> = {
  receipts?: Address<TAccountReceipts>;
  authority: TransactionSigner<TAccountAuthority>;
  storeName: MarkAsDeliveredInstructionDataArgs["storeName"];
  receiptId: MarkAsDeliveredInstructionDataArgs["receiptId"];
};

export async function getMarkAsDeliveredInstructionAsync<
  TAccountReceipts extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof LET_ME_BUY_PROGRAM_ADDRESS,
>(
  input: MarkAsDeliveredAsyncInput<TAccountReceipts, TAccountAuthority>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  MarkAsDeliveredInstruction<
    TProgramAddress,
    TAccountReceipts,
    TAccountAuthority
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? LET_ME_BUY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    receipts: { value: input.receipts ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.receipts.value) {
    accounts.receipts.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([114, 101, 99, 101, 105, 112, 116, 115]),
        ),
        getUtf8Encoder().encode(
          getNonNullResolvedInstructionInput("storeName", args.storeName),
        ),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("receipts", accounts.receipts),
      getAccountMeta("authority", accounts.authority),
    ],
    data: getMarkAsDeliveredInstructionDataEncoder().encode(
      args as MarkAsDeliveredInstructionDataArgs,
    ),
    programAddress,
  } as MarkAsDeliveredInstruction<
    TProgramAddress,
    TAccountReceipts,
    TAccountAuthority
  >);
}

export type MarkAsDeliveredInput<
  TAccountReceipts extends string = string,
  TAccountAuthority extends string = string,
> = {
  receipts: Address<TAccountReceipts>;
  authority: TransactionSigner<TAccountAuthority>;
  storeName: MarkAsDeliveredInstructionDataArgs["storeName"];
  receiptId: MarkAsDeliveredInstructionDataArgs["receiptId"];
};

export function getMarkAsDeliveredInstruction<
  TAccountReceipts extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof LET_ME_BUY_PROGRAM_ADDRESS,
>(
  input: MarkAsDeliveredInput<TAccountReceipts, TAccountAuthority>,
  config?: { programAddress?: TProgramAddress },
): MarkAsDeliveredInstruction<
  TProgramAddress,
  TAccountReceipts,
  TAccountAuthority
> {
  // Program address.
  const programAddress = config?.programAddress ?? LET_ME_BUY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    receipts: { value: input.receipts ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("receipts", accounts.receipts),
      getAccountMeta("authority", accounts.authority),
    ],
    data: getMarkAsDeliveredInstructionDataEncoder().encode(
      args as MarkAsDeliveredInstructionDataArgs,
    ),
    programAddress,
  } as MarkAsDeliveredInstruction<
    TProgramAddress,
    TAccountReceipts,
    TAccountAuthority
  >);
}

export type ParsedMarkAsDeliveredInstruction<
  TProgram extends string = typeof LET_ME_BUY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    receipts: TAccountMetas[0];
    authority: TAccountMetas[1];
  };
  data: MarkAsDeliveredInstructionData;
};

export function parseMarkAsDeliveredInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedMarkAsDeliveredInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { receipts: getNextAccount(), authority: getNextAccount() },
    data: getMarkAsDeliveredInstructionDataDecoder().decode(instruction.data),
  };
}
