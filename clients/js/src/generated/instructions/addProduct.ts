/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { LET_ME_BUY_PROGRAM_ADDRESS } from "../programs";

export const ADD_PRODUCT_DISCRIMINATOR = new Uint8Array([
  0, 219, 137, 36, 105, 180, 164, 93,
]);

export function getAddProductDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(ADD_PRODUCT_DISCRIMINATOR);
}

export type AddProductInstruction<
  TProgram extends string = typeof LET_ME_BUY_PROGRAM_ADDRESS,
  TAccountReceipts extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountMint extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountReceipts extends string
        ? WritableAccount<TAccountReceipts>
        : TAccountReceipts,
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountMint extends string
        ? ReadonlyAccount<TAccountMint>
        : TAccountMint,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type AddProductInstructionData = {
  discriminator: ReadonlyUint8Array;
  storeName: string;
  name: string;
  price: bigint;
};

export type AddProductInstructionDataArgs = {
  storeName: string;
  name: string;
  price: number | bigint;
};

export function getAddProductInstructionDataEncoder(): Encoder<AddProductInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["storeName", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["name", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["price", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: ADD_PRODUCT_DISCRIMINATOR }),
  );
}

export function getAddProductInstructionDataDecoder(): Decoder<AddProductInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["storeName", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["name", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["price", getU64Decoder()],
  ]);
}

export function getAddProductInstructionDataCodec(): Codec<
  AddProductInstructionDataArgs,
  AddProductInstructionData
> {
  return combineCodec(
    getAddProductInstructionDataEncoder(),
    getAddProductInstructionDataDecoder(),
  );
}

export type AddProductAsyncInput<
  TAccountReceipts extends string = string,
  TAccountAuthority extends string = string,
  TAccountMint extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  receipts?: Address<TAccountReceipts>;
  authority: TransactionSigner<TAccountAuthority>;
  mint: Address<TAccountMint>;
  systemProgram?: Address<TAccountSystemProgram>;
  storeName: AddProductInstructionDataArgs["storeName"];
  name: AddProductInstructionDataArgs["name"];
  price: AddProductInstructionDataArgs["price"];
};

export async function getAddProductInstructionAsync<
  TAccountReceipts extends string,
  TAccountAuthority extends string,
  TAccountMint extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof LET_ME_BUY_PROGRAM_ADDRESS,
>(
  input: AddProductAsyncInput<
    TAccountReceipts,
    TAccountAuthority,
    TAccountMint,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  AddProductInstruction<
    TProgramAddress,
    TAccountReceipts,
    TAccountAuthority,
    TAccountMint,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? LET_ME_BUY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    receipts: { value: input.receipts ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: true },
    mint: { value: input.mint ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.receipts.value) {
    accounts.receipts.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([114, 101, 99, 101, 105, 112, 116, 115]),
        ),
        getUtf8Encoder().encode(
          getNonNullResolvedInstructionInput("storeName", args.storeName),
        ),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("receipts", accounts.receipts),
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("mint", accounts.mint),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getAddProductInstructionDataEncoder().encode(
      args as AddProductInstructionDataArgs,
    ),
    programAddress,
  } as AddProductInstruction<
    TProgramAddress,
    TAccountReceipts,
    TAccountAuthority,
    TAccountMint,
    TAccountSystemProgram
  >);
}

export type AddProductInput<
  TAccountReceipts extends string = string,
  TAccountAuthority extends string = string,
  TAccountMint extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  receipts: Address<TAccountReceipts>;
  authority: TransactionSigner<TAccountAuthority>;
  mint: Address<TAccountMint>;
  systemProgram?: Address<TAccountSystemProgram>;
  storeName: AddProductInstructionDataArgs["storeName"];
  name: AddProductInstructionDataArgs["name"];
  price: AddProductInstructionDataArgs["price"];
};

export function getAddProductInstruction<
  TAccountReceipts extends string,
  TAccountAuthority extends string,
  TAccountMint extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof LET_ME_BUY_PROGRAM_ADDRESS,
>(
  input: AddProductInput<
    TAccountReceipts,
    TAccountAuthority,
    TAccountMint,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): AddProductInstruction<
  TProgramAddress,
  TAccountReceipts,
  TAccountAuthority,
  TAccountMint,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? LET_ME_BUY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    receipts: { value: input.receipts ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: true },
    mint: { value: input.mint ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("receipts", accounts.receipts),
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("mint", accounts.mint),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getAddProductInstructionDataEncoder().encode(
      args as AddProductInstructionDataArgs,
    ),
    programAddress,
  } as AddProductInstruction<
    TProgramAddress,
    TAccountReceipts,
    TAccountAuthority,
    TAccountMint,
    TAccountSystemProgram
  >);
}

export type ParsedAddProductInstruction<
  TProgram extends string = typeof LET_ME_BUY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    receipts: TAccountMetas[0];
    authority: TAccountMetas[1];
    mint: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: AddProductInstructionData;
};

export function parseAddProductInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedAddProductInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      receipts: getNextAccount(),
      authority: getNextAccount(),
      mint: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getAddProductInstructionDataDecoder().decode(instruction.data),
  };
}
