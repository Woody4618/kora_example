/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getProductsDecoder,
  getProductsEncoder,
  getReceiptDecoder,
  getReceiptEncoder,
  type Products,
  type ProductsArgs,
  type Receipt,
  type ReceiptArgs,
} from "../types";

export const RECEIPTS_DISCRIMINATOR = new Uint8Array([
  222, 245, 237, 64, 59, 49, 29, 246,
]);

export function getReceiptsDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(RECEIPTS_DISCRIMINATOR);
}

export type Receipts = {
  discriminator: ReadonlyUint8Array;
  receipts: Array<Receipt>;
  totalPurchases: bigint;
  storeName: string;
  authority: Address;
  products: Array<Products>;
  telegramChannelId: string;
  bump: number;
  details: string;
};

export type ReceiptsArgs = {
  receipts: Array<ReceiptArgs>;
  totalPurchases: number | bigint;
  storeName: string;
  authority: Address;
  products: Array<ProductsArgs>;
  telegramChannelId: string;
  bump: number;
  details: string;
};

/** Gets the encoder for {@link ReceiptsArgs} account data. */
export function getReceiptsEncoder(): Encoder<ReceiptsArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["receipts", getArrayEncoder(getReceiptEncoder())],
      ["totalPurchases", getU64Encoder()],
      ["storeName", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["authority", getAddressEncoder()],
      ["products", getArrayEncoder(getProductsEncoder())],
      [
        "telegramChannelId",
        addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder()),
      ],
      ["bump", getU8Encoder()],
      ["details", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
    ]),
    (value) => ({ ...value, discriminator: RECEIPTS_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link Receipts} account data. */
export function getReceiptsDecoder(): Decoder<Receipts> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["receipts", getArrayDecoder(getReceiptDecoder())],
    ["totalPurchases", getU64Decoder()],
    ["storeName", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["authority", getAddressDecoder()],
    ["products", getArrayDecoder(getProductsDecoder())],
    [
      "telegramChannelId",
      addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder()),
    ],
    ["bump", getU8Decoder()],
    ["details", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
  ]);
}

/** Gets the codec for {@link Receipts} account data. */
export function getReceiptsCodec(): Codec<ReceiptsArgs, Receipts> {
  return combineCodec(getReceiptsEncoder(), getReceiptsDecoder());
}

export function decodeReceipts<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<Receipts, TAddress>;
export function decodeReceipts<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<Receipts, TAddress>;
export function decodeReceipts<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<Receipts, TAddress> | MaybeAccount<Receipts, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getReceiptsDecoder(),
  );
}

export async function fetchReceipts<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<Receipts, TAddress>> {
  const maybeAccount = await fetchMaybeReceipts(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeReceipts<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<Receipts, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeReceipts(maybeAccount);
}

export async function fetchAllReceipts(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<Receipts>[]> {
  const maybeAccounts = await fetchAllMaybeReceipts(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeReceipts(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<Receipts>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeReceipts(maybeAccount));
}
